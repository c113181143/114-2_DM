W14-練習

##價值

#erro1：

這個錯誤表示df.corr（）函數在計算相關性時，嘗試處理非數值的欄位，特別是「銷售」欄位，它是一個字串型別。 corr（）函數只接受數值型資料。 為了解決這個問題，我們需要先從DataFrame中選擇出所有數值型欄位，然後再計算相關性。 你好！ 該代碼已成功執行，併產生了相關性矩陣的熱圖。 熱圖現在應該顯示在輸出中。

#sns.heatmap(df.corr().round(2), annot=True, cmap='coolwarm');

sns.heatmap(df.select_dtypes(include=np.number).corr().round(2), annot=True, cmap='coolwarm');
這行程式碼 plt.plot(fpr, tpr); 是用來繪製「精確率-召回率曲線（精確召回曲線）」。

precision_recall_curve 函數計算了在不同門檻值（閾值）下的精確率（精度）和召回率（召回）。
fpr在本上下文中實際上代表了精確率（精度）值。
tpr在這個上下文中實際上代表了召回率（召回）值。
閾值是分類器在決定分類時使用的機率門檻值。 這條曲線顯示了分類器在不同精確率和召回率之間的權衡，對於評估不平衡資料集的模型效能特別有用。 注意，這裡的fpr和tpr變數名稱是使用繪製ROC曲線時的變數名，但它們在這裡代表的是精確率和召回率。
#erro2：這個錯誤表示corrwith（）函數在計算相關性時，嘗試處理非數值的欄位，特別是「銷售」欄位，它是一個字串型別。 corrwith（）函數只接受數值型資料。 為了解決這個問題，我們需要先從DataFrame中，將「左」欄位移除後，再選擇所有數值型欄位，然後再計算它與「左」欄位的相關性。

df.drop('left', axis=1).corrwith(df['left']).round(2)

df.drop('left', axis=1).select_dtypes(include=np.number).corrwith(df['left']).round(2)
這行程式碼df.drop('left', axis=1).select_dtypes(include=np.number).corrwith(df['left']).round(2)的作用是計算DataFrame中所有數值型特徵與'left'欄位之間的相關性，並將結果四捨五入到小數點後兩位。

具體來說，它執行了以下步驟：

df.drop（'left'，axis=1）：首先從原始DataFrame df 中移除'left' 欄位。 axis=1 表示移除欄位而不是列。

.select_dtypes（include=np.number）：接著，從剩餘的資料框架中選取所有資料類為數值的欄位。 這是為了解決之前可能出現的非數值資料導致的錯誤。
.corrwith（df['left']）：然後，計算這些選定的數值型欄位與原始資料框架中的「左」欄位（目標變量）之間的皮爾遜相關係數。 這會返回一個系列，其中索引是數值型欄位名稱，值是它們與「左」的相關係數。-.round(2)：最後，將計算出來的所有相關係數四舍五入到小數點後兩位，使結果更易於閱讀。
#erro3：這個錯誤 AttributeError：'float'物件沒有屬性'round'表示在一個浮點數物件上找不到round() 方法。 雖然Python的標準浮點數物件確實有.round（）方法，但在某些情況下，這個錯誤可能會出現。 為了解決這個問題並確保程式的穩定性，建議使用Python內建的round（）函數，它可以更好地處理各種數值型別的四捨五入。 我將把程式碼修改為round（accuracy_score（y_test，y_pred），2）。

from sklearn.dummy import DummyClassifier
from sklearn.metrics import confusion_matrix, classification_report, accuracy_score
dmy = DummyClassifier(strategy='most_frequent')
dmy.fit(X_train, y_train)
dmy.score(X_train, y_train)
y_pred = dmy.predict(X_test)
print('正確率：', accuracy_score(y_test, y_pred).round(2))
print('混亂矩陣')
print(confusion_matrix(y_test, y_pred))
print('綜合報告')
print(classification_report(y_test, y_pred))

from sklearn.dummy import DummyClassifier
from sklearn.metrics import confusion_matrix, classification_report, accuracy_score
dmy = DummyClassifier(strategy='most_frequent')
dmy.fit(X_train, y_train)
dmy.score(X_train, y_train)
y_pred = dmy.predict(X_test)
print('正確率：', round(accuracy_score(y_test, y_pred), 2))
print('混亂矩陣')
print(confusion_matrix(y_test, y_pred))
print('綜合報告')
print(classification_report(y_test, y_pred))
您好，這段程式碼H6zkLtj_wiQZ我上一輪已經詳細解釋過了。 簡單來說，它使用DummyClassifier建立一個最簡單的標準分類器，這個分類器總是預測訓練集中出現最多次的類別。 然後，它計算並列印了這個基準模型在測試集中的「正確率」、「混亂矩陣」和「綜合報告」。 這樣做是為了有一個參考點，以便後續比較更複雜模型的效能。

#erro4：這個錯誤 AttributeError：'float'物件沒有屬性'round' 表示您正在嘗試在一個浮點數物件上呼叫round() 方法，但round()在Python中是一個內建函式數，而不是浮點數物件的方法。 正確的做法是使用內建的round（）函數，將浮點數作為第一個參數，要保留的小數位數作為第二個參數。 我會將score.round(3) 修改為round(score, 3)。

# 載入所有模型
from sklearn.linear_model import LogisticRegression
from sklearn.svm import SVC
from sklearn.neighbors import KNeighborsClassifier
from sklearn.tree import DecisionTreeClassifier
from sklearn.ensemble import RandomForestClassifier, BaggingClassifier, AdaBoostClassifier
from xgboost import XGBClassifier
# 載作Pipeline，PCA和GridSearchCV
from sklearn.pipeline import Pipeline
from sklearn.model_selection import GridSearchCV

model_pl = Pipeline([
    ('preprocess', data_pl),
    ('model', LogisticRegression())
])
param_grid = {'model':[LogisticRegression(), SVC(), 
              KNeighborsClassifier(), DecisionTreeClassifier(max_depth=10)]}
gs = GridSearchCV(model_pl, param_grid=param_grid,
                  cv=5, return_train_score=True)
gs.fit(X_train, y_train)
score = gs.best_estimator_.score(X_test, y_test)
print('最佳預測參數', gs.best_params_)
print('訓練集交叉驗證的最佳結果', gs.best_score_.round(3))
print('測試集的結果', score.round(3))
y_pred = gs.best_estimator_.predict(X_test)
print('混亂矩陣\n',confusion_matrix(y_test, y_pred))

# 載入所有模型
from sklearn.linear_model import LogisticRegression
from sklearn.svm import SVC
from sklearn.neighbors import KNeighborsClassifier
from sklearn.tree import DecisionTreeClassifier
from sklearn.ensemble import RandomForestClassifier, BaggingClassifier, AdaBoostClassifier
from xgboost import XGBClassifier
# 載作Pipeline，PCA和GridSearchCV
from sklearn.pipeline import Pipeline
from sklearn.model_selection import GridSearchCV

model_pl = Pipeline([
    ('preprocess', data_pl),
    ('model', LogisticRegression())
])
param_grid = {'model':[LogisticRegression(), SVC(),
              KNeighborsClassifier(), DecisionTreeClassifier(max_depth=10)]}
gs = GridSearchCV(model_pl, param_grid=param_grid,
                  cv=5, return_train_score=True)
gs.fit(X_train, y_train)
score = gs.best_estimator_.score(X_test, y_test)
print('最佳預測參數', gs.best_params_)
print('訓練集交叉驗證的最佳結果', gs.best_score_.round(3))
print('測試集的結果', round(score, 3))
y_pred = gs.best_estimator_.predict(X_test)
print('混亂矩陣\n',confusion_matrix(y_test, y_pred))
單元格0WNWjPftwiQb中的程式執行網格搜尋交叉驗證，以在Logistic迴歸、SVM、K-最近鄰居和決策樹中找到最佳模型。 然後，它評估測試集上最佳模型的效能。

以下是輸出明細：

最佳預測參數（最佳預測引數）：這表明GridSearchCV發現DecisionTreeClassifier（max_depth=10）是測試模型中最好的模型。 訓練集交叉驗證的最佳結果（訓練集交叉驗證的最佳結果）：在訓練集交叉驗證期間，DecisionTreeClassifier（max_depth=10）實現的最佳平均準確性約為0.979。 測試集的結果（測試集結果）：在看不見的測試資料上，所選的DecisionTreeClassifier的最終準確性約為0.978。 混亂矩陣（混亂矩陣）：[[3396 32] [ 69 1003]]該矩陣總結了測試集分類模型的效能：3396（真負數）：正確預測為沒有離開的員工人數。 32（假陽性）：錯誤預測離職的員工人數（實際上他們並沒有離職）。 69（假陰性）：錯誤地預測為沒有離開的員工人數（他們實際上確實離開了）。 1003（True Positives）：正確預測離職的員工人數。

#erro5：錯誤AttributeError：「float」物件沒有屬性「round」，這意味著您正在嘗試使用.round（）作為浮點數的方法，但它是一個內建的Python函式。 要解決這個問題，您需要直接使用round（）函式，如round（score，3）。

model_pl = Pipeline([
    ('preprocess', data_pl),
    ('model', LogisticRegression())
])
np.random.seed(42)
param_grid = {'model':[RandomForestClassifier(), AdaBoostClassifier(), 
                       BaggingClassifier(), XGBClassifier()]}
gs = GridSearchCV(model_pl, param_grid=param_grid,
                  cv=5, return_train_score=True)
gs.fit(X_train, y_train)
score = gs.best_estimator_.score(X_test, y_test)
print('最佳預測參數', gs.best_params_)
print('訓練集交叉驗證的最佳結果', gs.best_score_.round(3))
print('測試集的結果', score.round(3))
y_pred = gs.best_estimator_.predict(X_test)
print('混亂矩陣\n',confusion_matrix(y_test, y_pred))

model_pl = Pipeline(
    [('preprocess', data_pl), ('model', LogisticRegression())]
)
np.random.seed(42)
param_grid = {
    'model': [
        RandomForestClassifier(),
        AdaBoostClassifier(),
        BaggingClassifier(),
        XGBClassifier(),
    ]
}
gs = GridSearchCV(model_pl, param_grid=param_grid, cv=5, return_train_score=True)
gs.fit(X_train, y_train)
score = gs.best_estimator_.score(X_test, y_test)
print('最佳預測參數', gs.best_params_)
print('訓練集交叉驗證的最佳結果', gs.best_score_.round(3))
print('測試集的結果', round(score, 3))
y_pred = gs.best_estimator_.predict(X_test)
print('混亂矩陣\n', confusion_matrix(y_test, y_pred))
單元格xUcv8-cowiQe中的程式旨在使用GridSearchCV為您的員工流失預測任務找到最佳效能的合奏模型。 讓我為您分解：model_pl = Pipeline(...)：這建立了一個管道。 它首先應用data_pl（這是您的ColumnTransformer，用於預處理數值和分類特徵），然後是初始LogisticRegression模型。 此LogisticRegression充當佔位符，因為要測試的實際模型將來自param_grid。np.random.seed(42)：這設定了一個隨機種子，以確保涉及隨機性的模型（如一些合奏方法）的結果是可重複的。param_grid = {'model': [...]}:這定義了要搜尋的超引數網格。 在這種情況下，它正在測試不同的機器學習模型：RandomForestClassifier、AdaBoostClassifier、BaggingClassifier和XGBClassifier。 GridSearchCV將使用預處理管道嘗試這些模型。gs = GridSearchCV(...)：這初始化了GridSearchCV：

model_pl：訓練和評估的管道。
param_grid：定義要測試的模型的字典。
cv=5：指定5倍交叉驗證，這意味著訓練資料將分為5個部分，模型將訓練5次，每次使用不同的部分進行驗證，其餘部分用於訓練。
return_train_score=True：這確保了訓練分數也被記錄下來，這對於檢測過度擬合很有用。
gs.fit（X_train，y_train）：這是GridSearchCV執行的地方。 它使用X_train和y_train資料的交叉驗證來訓練和評估param_grid中指定的每個模型。
分數=gs.best_estimator_.score（X_test，y_test）：在找到最佳模型（基於交叉驗證效能）後，此行在完全看不見的X_test和y_test資料上評估最佳模型，以獲得最終的效能分數。
print('最佳預測參數', gs.best_params_)：列印在網格搜尋期間產生最佳效能的超引數（在這種情況下，是特定模型）。
print('訓練集交叉驗證的最佳結果', gs.best_score_.round(3))：列印訓練集中最佳模型所取得的最佳交叉驗證分數，四捨五入到小數點後三位。
print（'測試集的結果'，round（score，3））：列印測試集中最佳模型的分數，四捨五入到小數點後三位。
y_pred = gs.best_estimator_.predict(X_test)：使用最佳模型對測試集進行預測。
rint('混亂矩陣\n', confusion_matrix(y_test, y_pred))：列印混淆矩陣，該矩陣提供了模型預測與測試集實際值的詳細細分。
